# ====================================================================
# SAFETY: Franka collision/reflex thresholds
# ====================================================================
# Applied via robot.setCollisionBehavior() when
# use_custom_collision_behavior = 1. Torque thresholds are joint-side [Nm] and
# force thresholds are Cartesian [N/Nm], each set equal across all 7/6 axes.
# Finite, not a safety bypass -- increase only under supervision.
#
# Cross-check against the set-up press: the peak preload force is roughly
# setup_Kp_z * setup_push_end (sequence.txt), and it must stay below the
# force thresholds here or the reflex will trip mid-run.
use_custom_collision_behavior = 1
collision_torque_acc = 140.0
collision_torque_nom = 140.0
collision_force_acc = 240.0
collision_force_nom = 240.0
