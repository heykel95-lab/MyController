run_id:   VALID_T2_t2_10deg
repeats:  3

plane profile: tilted

tool profile: grinding_tool

purpose:
  Tilted-plane validation at the baseline gains with independent tool offsets (t1=+0 deg, t2=+10 deg).

pass criterion:
  Compare with the matched horizontal Case-A result using the measured first-contact angle. This is a frame-transfer check, not a second parameter sweep.
