run_id:   VALID_T1_t1_10deg
repeats:  3

plane profile: tilted

purpose:
  Tilted-plane validation at the baseline gains with independent tool offsets (t1=+10 deg, t2=+0 deg).

pass criterion:
  Compare with the matched horizontal Case-A result using the measured first-contact angle. This is a frame-transfer check, not a second parameter sweep.
