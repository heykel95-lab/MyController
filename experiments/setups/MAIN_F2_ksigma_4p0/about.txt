run_id:   MAIN_F2_ksigma_4p0
repeats:  1

plane profile: horizontal

tool profile: grinding_tool

purpose:
  Null-space mode 2 under the 20 N link-3 point-force disturbance: smallest-singular-value bias only, k_sigma = 4.0.

pass criterion:
  Accept a setting only if the commanded null-space direction is actually followed by joint motion and the Cartesian task drift stays within its registered limit. Torque without motion is below the friction threshold, not optimisation.
